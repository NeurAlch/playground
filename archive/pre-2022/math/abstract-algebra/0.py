# Non-Empty Set
#   -> Semigroup (has a binary associative operation, with a closure)
#   -> Monoid (has an identity element)
#   -> Group (each element has an inverse)
# Non-Empty Set
#   -> Semigroup (has a binary associative operation, with a closure)
#   -> Ring (has two binary associative operations, addition and multiplication)